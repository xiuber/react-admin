import React from 'react';
import './LinkPoint.less';

export default function LinkPoint(props) {
    const {
        size,
        x,
        y,
    } = props;

    return (
        <div styleName="root">

        </div>
    );
}
